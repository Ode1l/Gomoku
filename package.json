{
  "name": "p2p-lockstep-gomoku",
  "version": "0.1.0",
  "private": true,
  "description": "Peer-to-peer Gomoku web application.",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm typecheck && vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "p2p-lockstep-kit-ui": "^0.1.2"
  },
  "devDependencies": {
    "p2p-lockstep-kit-session": "^0.1.12",
    "typescript": "^5.9.3",
    "vite": "^8.1.0",
    "vitest": "^4.1.2",
    "wrangler": "^4.105.0"
  }
}
